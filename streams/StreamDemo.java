package com.ajayganesh.streams;

public class StreamDemo {
  private StreamDemo() {}

  public static void show() {
    // IntStream.range(1, 5).forEach(n -> System.out.println(n));

    // IntStream.rangeClosed(1, 5).forEach(n -> System.out.println(n));

    // IntStream.of(1, 2, 3, 4, 5).forEach(n -> System.out.println(n));

    // IntStream.iterate(1, n -> n + 1).limit(5).forEach(n -> System.out.println(n));

    // IntStream.generate(() -> Math.random()).limit(5).forEach(n -> System.out.println(n));

    // IntStream.generate(() -> 1).limit(5).forEach(n -> System.out.println(n));
  }
}
